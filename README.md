# Misc


````
sudo dd bs=4M if=source of=destion conv=fsync oflag=direct status=progress
````
clone 

````
rsync -ah --progress source destionation
````
copy to remote

````
du -h --max-depth=1
````
List the size (in human readable form) of all sub folders from the current location




